#ifndef ENUMS_HPP
#define ENUMS_HPP

enum {SEE_ALL, SEE_LAST, SEE_NOTHING, SEE_COUNT};

enum {CLASSIC, UNO};
enum {Valet = 11, Dame = 12, Roi = 13, As = 14};
enum {SPADE, HEART, DIAMOND, CLOVER};
enum {PlusDeux = 11, InversementDeSens = 12, PasseTonTour = 13, PlusQuatre = 14, Joker = 15};
enum {Bleu, Rouge, Jaune, Vert};
enum {startOfTurn = 100, endOfTurn = 200};

enum {
    FG_RED     = 31,
    FG_GREEN   = 32,
    FG_YELLOW  = 33,
    FG_BLUE    = 34,
    FG_DEFAULT = 39
};

#endif